/**
 * Represents a character in the application
 * @interface Character
 * @property {string} name - The character's name
 * @property {string} notes - Any notes about the character
 */
export interface Character {
    name: string;
    notes: string;
} 